<section id="details-page">
  {{#with creatures}}
  <div class="main_card">
    <div class="card_left">
      <div class="card_datails">
        <h1>Name: {{name}}</h1>
        <h3>Created by an author: {{creator}}</h3>
        <div class="card_character">
          <p class="card-species">Species: {{species}}</p>
          <p class="card-skin">Skin colour: {{skincolor}}</p>
          <p class="card-eye">Eye Colour: {{eyecolor}}</p>
        </div>

        <p class="disc">
          Description:
          {{description}}
        </p>

        <div class="social-btn">
        {{#if isOwner}}
          <a href="/creature/{{_id}}" class="edit-btn">Edit</a>
          <a href="/creature/{{_id}}" class="del-btn">Delete</a>
          {{else}}
          <!-- logged in users, who have not yet voted-->
          <a href="/creature/{{_id}}" class="vote-up">Vote</a>
        {{/if}}

          <!-- logged in user who has already voted-->
          <p class="thanks-for-vote">Thanks For Voting</p>
        </div>
      </div>
    </div>
    <div class="card_right">
      <img src="{{image}}" alt="image" />
    </div>
  </div>
</section>

<section id="votes">
  <div class="vote-info">
    <div class="card_left">
      <div class="card_datails">
        <h1>Votes</h1>
        <div class="card_vote">
          <!-- Show the rating of votes, if there are no votes yet, the number must be 0. -->
          <p class="PV">Total rating of votes: {{votes}}</p>
        </div>
        <!-- If there are already people who have cast their vote for the post, separate their emails with a comma and a space ", " -->
        <!-- <p class="disc">People who voted for the post - alex@gmai.com, petar@gmail.com, petq@gmail.com</p>-->

        <!-- If not display: -->
        <p class="disc">
          People who voted for the post - No one has voted yet.
        </p>
      </div>
    </div>
  </div>

  {{/with}}
</section>